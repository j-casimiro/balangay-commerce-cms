# Prisma reads DATABASE_URL from this file when running CLI commands
# (migrate / generate / seed / studio) from the packages/database directory.
DATABASE_URL=postgresql://cms:cms_password@localhost:5432/ecommerce_cms?schema=public
